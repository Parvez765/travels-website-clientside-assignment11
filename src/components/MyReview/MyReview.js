import React from 'react';
import useTitle from '../../hooks/useTitle';

const MyReview = () => {
    useTitle("My Review")
    return (
        <div>
           <h2>Hello</h2> 
        </div>
    );
};

export default MyReview;